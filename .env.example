# Jira Data Center (on-premise)
JIRA_URL="https://jira.omgeving.vlaanderen.be/jira"
JIRA_PERSONAL_TOKEN=your_personal_access_token_here
JIRA_SSL_VERIFY=true

# Project key voor flux-web-components (pas aan)
JIRA_PROJECT_KEY=FLUX

# State folder — aparte locatie, idealiter een eigen git repo (zie README).
# Verwacht als sibling naast deze repo. Kan ook een absoluut pad zijn.
STATE_DIR=../flux-agents-state

# flux-web-components repo die de agents beheren. Bij eerste run wordt
# hier een clone gemaakt onder state/repo/flux-web-components/ (gitignored).
# Volledig los van jouw eigen werk-clone, zodat een refine- of develop-run
# nooit jouw branches of staging raakt.
FLUX_REPO_URL=git@github.com:milieuinfo/flux-web-components.git
FLUX_BASE_BRANCH=develop-v2
# Optioneel: override de locatie van de managed clone
# FLUX_REPO_DIR=./state/repo/flux-web-components

# Model selectie
# - claude-opus-4-7 voor agent 1 (refine) en 2 (plan) - analyse-zwaar
# - claude-sonnet-4-6 voor snelle runs / kostenbesparing
AGENT1_MODEL=claude-opus-4-7
AGENT2_MODEL=claude-opus-4-7
AGENT3_MODEL=claude-sonnet-4-6
AGENT4_MODEL=claude-opus-4-7

# Max beurten per agent-run (override indien een ticket telkens tegen
# de limiet loopt met error_max_turns).
# AGENT1_MAX_TURNS=30
# AGENT3_MAX_TURNS=60
# AGENT4_MAX_TURNS=50

# Harde timeout per Bash-call (ms) in develop/review/review-external.
# Wordt afgedwongen via een PreToolUse-hook: ontbreekt `timeout` in de
# call of is hij hoger, dan clampen we naar deze waarde. SDK-cap = 600s.
# AGENT_BASH_TIMEOUT_MS=600000

# Refine-agent leest image-attachments (jpeg/png/gif/webp) van het ticket
# en geeft ze door als vision content blocks. Limieten beschermen tegen
# token-budget-explosie bij tickets met veel of zware screenshots.
# JIRA_REFINE_IMAGE_MAX_COUNT=5
# JIRA_REFINE_IMAGE_MAX_BYTES=5000000

# Auteur van git-commits gemaakt door agent 3 (develop, per ronde) en
# agent 4 (review, squash bij APPROVED). Defaults in shared/repo.ts
# (Kris Speltincx, kris.speltincx@vlaanderen.be); zet hier alleen als je
# een andere identiteit wil.
# FLUX_GIT_AUTHOR_NAME=Kris Speltincx
# FLUX_GIT_AUTHOR_EMAIL=kris.speltincx@vlaanderen.be

# Optioneel: als je NIET via je Claude Code MAX sessie wil authenticeren
# ANTHROPIC_API_KEY=sk-ant-...

# Publish-specifieke velden (alleen relevant voor `npm run publish`)
# - JIRA_SPRINT_FIELD: customfield-key voor de sprint-array op een issue
#   (default customfield_10020). Wijkt af tussen Jira instances.
# - JIRA_STORYPOINTS_FIELD: customfield-key voor story points (optioneel).
# - JIRA_REALIZATION_LINK_TYPE: exacte naam van het issue-link-type voor
#   "Wordt gerealiseerd door" (bv. Realization). Leeg laten = auto-detect
#   via inward-description.
# - JIRA_UMBRELLA_EPIC: epic waaraan het [Sprint-analyse]-ticket wordt
#   gehangen. Mag een issue-key zijn (bv. FLUX-42) of een Epic Name
#   (bv. "[2026] - samenwerking"). Leeg laten = geen epic-link.
# - JIRA_EPIC_LINK_FIELD / JIRA_EPIC_NAME_FIELD: customfield-keys voor
#   Epic Link en Epic Name. Leeg laten = auto-detect via /rest/api/2/field.
# JIRA_STORYPOINTS_FIELD=customfield_10004
# JIRA_REALIZATION_LINK_TYPE=Realization
# JIRA_EPIC_LINK_FIELD=customfield_10014
# JIRA_EPIC_NAME_FIELD=customfield_10011
JIRA_SPRINT_FIELD=customfield_10111
JIRA_UMBRELLA_EPIC="[2026] - samenwerking"

# Log level: debug | info | warn
LOG_LEVEL=info
